# Streamlit secrets configuration (local development)
# For production, add these secrets in Streamlit Cloud dashboard

# Backend API URL
api_url = "http://127.0.0.1:8000"

# After deploying backend to Vercel, replace with:
# api_url = "https://your-project-name.vercel.app"
